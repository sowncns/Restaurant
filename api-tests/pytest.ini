[pytest]
addopts = -ra --strict-markers -m "not destructive"
testpaths = tests
markers =
    contract: public API response contracts
    auth: authentication and authorization behavior
    smoke: authenticated safe-read smoke tests
    destructive: opt-in tests which create or mutate test data
    business_flow: complete restaurant business workflow tests
    catalog: route catalog mounting and protection coverage
